export enum Patterns {
   // structural
   Adapter,
   Bridge,
   Composite,
   Decorator,
   Facade,
   Flyweight,
   Proxy,
   // behavioral
   ChainOfResponsibility,
   Command,
   Iterator,
   Mediator,
   Memento,
   Observer,
   State,
   Strategy,
   Template,
   Visitor,
   // creational
   Factory,
   AbstractFactory,
   Builder,
   Prototype,
   Singleton = "Singleton",
}